{{- if .Values.backend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "docgen.backend.fullname" . }}
  labels:
    {{- include "docgen.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
  annotations:
    docgen.io/cluster-dns: {{ include "docgen.backend.serviceFqdn" . | quote }}
    docgen.io/service-port: {{ .Values.backend.service.port | quote }}
spec:
  type: {{ .Values.backend.service.type }}
  selector:
    {{- include "docgen.backend.selectorLabels" . | nindent 4 }}
    {{- if .Values.blueGreen.enabled }}
    docgen.io/deployment-color: {{ .Values.blueGreen.activeColor | quote }}
    {{- end }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
