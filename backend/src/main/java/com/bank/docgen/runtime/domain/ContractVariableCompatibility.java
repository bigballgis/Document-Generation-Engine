package com.bank.docgen.runtime.domain;

/**
 * Consumer-contract compatibility classification for version-level {@code variables[]} (IBL-A4).
 */
public enum ContractVariableCompatibility {
    BREAKING,
    NON_BREAKING
}
