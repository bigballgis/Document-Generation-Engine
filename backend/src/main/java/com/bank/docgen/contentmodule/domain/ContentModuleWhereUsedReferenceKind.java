package com.bank.docgen.contentmodule.domain;

/**
 * IBL-E6 / ADR-0067 — how a template references a content module in where-used.
 */
public enum ContentModuleWhereUsedReferenceKind {
    DIRECT,
    NESTED
}
