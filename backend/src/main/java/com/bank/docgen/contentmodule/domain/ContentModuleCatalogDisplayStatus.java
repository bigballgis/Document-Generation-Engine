package com.bank.docgen.contentmodule.domain;

/**
 * CE-U20 catalog list {@code status} query values — badge-aligned display status
 * of the module head version (not a separate persisted axis).
 */
public enum ContentModuleCatalogDisplayStatus {
    DRAFT,
    SUBMITTED,
    APPROVED,
    STOPPED,
    DEPRECATED
}
