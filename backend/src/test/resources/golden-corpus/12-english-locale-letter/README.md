# english-locale-letter (IBL-C3)

English (`en-US`) golden theme for F19 cross-locale matrix.

| Half | Provenance |
| --- | --- |
| DOCX | Structured assemble + compute (`FORMAT_AMOUNT` unary, `SPELL_AMOUNT` USD) |
| PDF | **SYNTHETIC** — PDFBox text projection from DOCX (honest; not LibreOffice) |

When `soffice` is available, a future IBL-D2 LO CI lane may upgrade selected halves to
`LIBREOFFICE`. This leaf does **not** invent LO-attributed PDF binaries.
