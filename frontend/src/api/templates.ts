/**
 * Template management API facade.
 * Domain modules: list / detail (+ panels) / lifecycle / bindings — call sites keep `@/api/templates`.
 */
export * from '@/api/templatesList'
export * from '@/api/templatesDetail'
export * from '@/api/templatesLifecycle'
export * from '@/api/templatesBindings'
